fastapi
uvicorn[standard]
pydantic-settings
sqlalchemy
pymysql
alembic
python-jose[cryptography]
passlib[bcrypt]
python-multipart
tensorflow
reportlab

# passlib 1.7.4 (non maintenu) est incompatible avec bcrypt>=4.1
# (suppression de l'attribut bcrypt.__about__). Version epinglee pour
# que le hachage des mots de passe fonctionne de maniere reproductible.
bcrypt==4.0.1
